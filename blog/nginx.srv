server {
    listen 80;
    listen [::]:80;
    server_name jellyfin.ethantwardy.com;

    # Redirect HTTP to HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name blog.ethantwardy.com;
    root /var/www/sitedata/blog;
}
