---
version: 1.0
configuration:
  - !server
    name: twardyece
    configuration:
      - !location |
        location ^~ /blog/ {
            root /var/www/sitedata;
            allow all;
        }
