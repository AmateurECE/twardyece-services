server {
    port_in_redirect on;
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name edtwardy.hopto.org;

    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

    ssl_certificate /etc/letsencrypt/live/edtwardy.hopto.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/edtwardy.hopto.org/privkey.pem;

    root /var/www/html;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
    }

    location /apps/ {
        uwsgi_pass edtwardy-webservices_apps_1:8000;
        include /etc/nginx/conf.d/uwsgi_params;
    }

    location /static/ {
        root /var/www;
    }

    # location /conan/ {
    #     proxy_pass http://127.0.0.1:9300/;
    # }
    # location /debian/ {
    #     autoindex on;
    # }
    # location /files {
    #     autoindex on;
    # }
    # location /pypi/ {
    #     proxy_set_header	X-Forwarded-Host $host:$server_port/pypi/;
    #     proxy_set_header	X-Forwarded-Proto $scheme;
    #     proxy_set_header	X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header	X-Real-IP $remote_addr;
    #     proxy_pass		http://127.0.0.1:52630/;
    # }

    # Redirect for repository
    # set $do_redirect 0;
    # if ($http_referer ~* /repository/) {
    #     set $do_redirect 1;
    # }
    # if ($request_uri ~* /repository/) {
    #     set $do_redirect 0;
    # }
    # if ($do_redirect = 1) {
    #     rewrite ^/(.*)$ /repository/$1 redirect;
    # }

    # location /repository/ {
    #     index index.html;
    # }
    # Redirect for repository
    # set $do_redirect 0;
    # if ($http_referer ~* writings) {
    #     set $do_redirect 1;
    # }
    # if ($request_uri ~* writings) {
    #     set $do_redirect 0;
    # }
    # if ($do_redirect = 1) {
    #     rewrite ^/(.*)$ /writings/$1 redirect;
    # }

    # location ^~ /protected_writings/ {
    #     internal;
    #     alias /var/www/edtwardy.hopto.org/writings/;
    #     index index.html;
    # }

    # location ^~ /writings/images {
    #     alias /var/www/edtwardy.hopto.org/writings/images;
    # }
}

server {
    if ($host = edtwardy.hopto.org) {
        return 301 https://$host$request_uri;
        } # managed by Certbot


        listen 80;
        listen [::]:80;
        server_name edtwardy.hopto.org;
        return 404; # managed by Certbot
}
